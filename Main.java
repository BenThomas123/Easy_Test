import java.util.Scanner;

public class Main {
    
    private static String messageBuffer = "                    ";
    private static String menuMessage = messageBuffer + "Easy Test" + messageBuffer + "\n" + 
    messageBuffer + "Press any button" + messageBuffer;
    private static String loginOptionString = "Login";
    private static String registerOptionString = "register";
    private static String aboutOption = "";
    private static Scanner scanner = new Scanner(System.in);


    public static void main(String[] args) {
        System.out.println(menuMessage);
        scanner.nextLine();

        // option
            //create a new account
                // have the user enter there credentials
                // search the database
            //login
            //info
        


    }
}
